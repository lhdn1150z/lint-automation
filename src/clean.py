"""Well-formatted module with matching unit tests."""


def multiply(a: float, b: float) -> float:
    """Return the product of a and b."""
    return a * b


def is_even(n: int) -> bool:
    """Return True if n is even."""
    return n % 2 == 0
